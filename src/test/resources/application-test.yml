spring:
  datasource:
    url: jdbc:h2:file:./testdb;DB_CLOSE_DELAY=-1;AUTO_RECONNECT=TRUE;DB_CLOSE_ON_EXIT=FALSE;
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

logging:
  level:
    liquibase: debug
